// Place all the styles related to the contents controller here.
// They will automatically be included in application.css.
// You can use Sass (SCSS) here: http://sass-lang.com/

//body {
 // background: url('http://dl.dropbox.com/u/17538030/paper2.jpg');
//}
#slider{
     background: #fff  no-repeat 50% 50%;
}
#slider img { display:none; }

#logo {
  margin-top: 30px;
}

.flexslider{
-webkit-transform: rotate(2deg); /* 回転 */
  -moz-transform: rotate(2deg);
  -ms-transform: rotate(2deg);
  transform: rotate(2deg);
}
body {
  background: url(<%= asset_path('retina_wood.png') %>) ;
}

.rice-paper{
  background: url(<%= asset_path('ricepaper2.png') %>) ;
}

.paper-3{
  background: url(<%= asset_path('paper_3.png') %>) ;
}


div.paper {
  margin: 20px 0;
  padding: 20px 20px;
  position: relative;
 // -webkit-box-shadow: 0 10px 6px -6px #777;
//  -moz-box-shadow: 0 10px 6px -6px #777;
//  box-shadow: 0 10px 6px -6px #777;
  
}


.drop-shadow {
   position:relative;
  // width:90%;
}

.drop-shadow:before,
.drop-shadow:after {
   content:"";
   position:absolute;
   z-index: -2;
   bottom:15px;
   left:10px;
   width:60%;
   height:20%;
   //max-width:300px;
   -webkit-box-shadow:0 15px 10px rgba(0, 0, 0, 0.7);
   -moz-box-shadow:0 15px 10px rgba(0, 0, 0, 0.7);
   box-shadow:0 15px 10px rgba(0, 0, 0, 0.7);
   -webkit-transform:rotate(-1deg);
   -moz-transform:rotate(-1deg);
   -o-transform:rotate(-1deg);
   transform:rotate(-1deg);
}

.drop-shadow:after{
   right:10px;
   left:auto;
   -webkit-transform:rotate(1deg);
   -moz-transform:rotate(1deg);
   -o-transform:rotate(1deg);
   transform:rotate(1deg);
 }
